# ==============================================================================
# XDNS Go Client Configuration (Sample)
# This sample is written for the current Go client implementation.
# Every key below is read by code or intentionally kept as an active runtime knob.
# Profile below is biased toward lossy / high-latency links with many resolvers.
# ==============================================================================

# Optional built-in profile. Keep "default" here, or use one of the bundled
# paired preset files: client_config.speed.toml, client_config.survival.toml,
# client_config.tcp-survival.toml.
# CONFIG_PRESET = "default"

# ------------------------------------------------------------------------------
# 1) Tunnel Identity & Security
# What this section does:
# - Defines which tunnel domains this client will use.
# - Defines payload encryption and the shared key that must match the server.
# ------------------------------------------------------------------------------

# Tunnel domains used to build DNS queries.
# Must match server DOMAIN values exactly.
# At least one domain is required.
# All domains must be handled by the same server. Do not use different domains across multiple servers.
DOMAINS = ["v.domain.com"]

# DNS record types the client rotates over when building tunnel queries (A1,
# anti-fingerprinting). Names are case-insensitive. The tunnel payload always
# rides in the query name, so any of these work, and the server auto-accepts
# every type; specialized A/AAAA data answers are opt-in server features:
#   TXT   -> TXT chunks (default, highest capacity over recursive resolvers)
#   NULL  -> raw frame in the answer RDATA (high capacity, direct-resolver path)
#   HTTPS / SVCB -> frame inside a service-binding SvcParam value
#   A     -> IPv4 A records when A_RECORD_DATA_DELIVERY is enabled server-side
#   AAAA  -> denser IPv6 records when AAAA_RECORD_DATA_DELIVERY is enabled
#   CNAME and all other types -> CNAME target (falls back to TXT for large frames)
# Unset / empty = TXT only (historical behavior). Recognized:
#   TXT, CNAME, A, AAAA, NULL, MX, NS, PTR, SRV, SVCB, CAA, NAPTR, SOA, HTTPS
# Example: QUERY_TYPES = ["TXT", "CNAME", "NULL", "HTTPS"]
QUERY_TYPES = ["TXT"]

# When true, the duplicate copies of a packet (see *_PACKET_DUPLICATION_COUNT)
# are sent across distinct tunnel domains where possible, instead of just
# distinct resolvers (A6). This gives independent delivery paths on lossy links
# and spreads query volume across domains. Has no effect with a single domain or
# a duplication count of 1. Enabled by default because it adds no traffic.
DUPLICATION_PREFER_DISTINCT_DOMAINS = true

# Tier-1 loss reducer: when true, the per-packet duplication count is scaled up
# automatically from the configured base toward the number of copies needed to
# reach ADAPTIVE_DUPLICATION_TARGET_DELIVERY at the measured loss, capped at 8.
# (At very high loss the cap is hit and Reed-Solomon FEC, when enabled, takes
# over.) Default false.
ADAPTIVE_DUPLICATION = false
# Target per-packet delivery probability adaptive duplication aims for
# (0.5..0.999). Default 0.95.
ADAPTIVE_DUPLICATION_TARGET_DELIVERY = 0.95

# Encryption method for tunnel payloads.
# Allowed values:
# 0 = None
# 1 = XOR
# 2 = ChaCha20
# 3 = AES-128-GCM
# 4 = AES-192-GCM
# 5 = AES-256-GCM
# Must match server DATA_ENCRYPTION_METHOD.
DATA_ENCRYPTION_METHOD = 1

# Shared encryption key.
# Required on the client.
# Must match the server-side key file contents.
ENCRYPTION_KEY = ""

# ------------------------------------------------------------------------------
# 2) Local Proxy Listener
# What this section does:
# - Controls where the local client proxy listens.
# - Controls whether local SOCKS authentication is required.
# ------------------------------------------------------------------------------

# Local mode.
# Allowed values:
# "SOCKS5" = normal proxy mode for browsers/apps
# "TCP"    = raw TCP tunnel mode
PROTOCOL_TYPE = "SOCKS5"

# Local bind address for the client proxy.
# For local-only use, "127.0.0.1" is fine.
# On systems where apps prefer IPv6 localhost (for example some macOS setups),
# "localhost" can be a better choice.
# If you want other devices on your network to use this proxy, use "0.0.0.0"
# and enable SOCKS5_AUTH.
LISTEN_IP = "127.0.0.1"
LISTEN_PORT = 18000

# Local SOCKS5 authentication.
# This protects the local proxy itself, not the remote server.
SOCKS5_AUTH = false
SOCKS5_USER = "master_dns_vpn"
SOCKS5_PASS = "master_dns_vpn"

# ------------------------------------------------------------------------------
# 3) Local DNS Service
# What this section does:
# - Controls the optional built-in local DNS listener.
# - Controls local DNS cache size, TTL, pending timeout, and fragment assembly.
# ------------------------------------------------------------------------------

# When true, the client exposes a local DNS server on LOCAL_DNS_IP:LOCAL_DNS_PORT.
LOCAL_DNS_ENABLED = false
LOCAL_DNS_IP = "127.0.0.1"
LOCAL_DNS_PORT = 53

# Local DNS cache sizing.
# Must be >= 1.
LOCAL_DNS_CACHE_MAX_RECORDS = 10000

# TTL used by the local DNS cache in seconds.
# If <= 0, code falls back to a default.
LOCAL_DNS_CACHE_TTL_SECONDS = 14400.0

# Maximum time a local DNS request is allowed to remain pending before it is
# treated as expired locally.
LOCAL_DNS_PENDING_TIMEOUT_SECONDS = 60.0

# Timeout for reassembling fragmented DNS tunnel responses.
# Used when DNS_QUERY_RES packets arrive in multiple fragments.
# Clamped in code to [1s, 600s].
DNS_RESPONSE_FRAGMENT_TIMEOUT_SECONDS = 60.0

# Persist local DNS cache to disk.
LOCAL_DNS_CACHE_PERSIST_TO_FILE = true

# Flush interval for persisted local DNS cache.
LOCAL_DNS_CACHE_FLUSH_INTERVAL_SECONDS = 60.0

# ------------------------------------------------------------------------------
# 4) Resolver Selection, Duplication, and Health
# What this section does:
# - Chooses how resolvers are selected.
# - Controls packet duplication for lossy networks.
# - Controls stream-aware resolver failover.
# - Controls background recheck and auto-disable of bad resolvers.
# ------------------------------------------------------------------------------

# Resolver balancing strategy.
# Allowed values:
# 1 = Random
# 2 = Round Robin
# 3 = Least Loss
# 4 = Lowest Latency
# 5 = MTU-weighted (active-pool resolvers get traffic in proportion to their
#     download MTU, so faster resolvers carry proportionally more)
# Modes 3 and 4 use runtime feedback from sends/successes.
RESOLVER_BALANCING_STRATEGY = 3

# Resolver address-family policy:
#   "auto" (default) = actively prefer healthy IPv4 resolvers and keep every
#                      provided IPv6 resolver warm as an automatic fallback.
#   "dual"           = balance across healthy IPv4 and IPv6 resolvers together.
#   "ipv4"           = use only IPv4 resolver endpoints.
#   "ipv6"           = use only IPv6 resolver endpoints.
# Auto mode still probes IPv6 for MTU and health, so failover needs no restart.
RESOLVER_IP_MODE = "auto"

# How DNS queries reach resolvers:
#   "auto" (default) - probe over UDP/53 first; if NO resolver passes MTU
#                      testing, retry the whole fleet over DNS-over-TCP/53.
#                      Zero cost on UDP-working networks (TCP is never tried).
#   "udp"            - UDP only (legacy behavior).
#   "tcp"            - TCP/53 only, for networks that block or truncate UDP/53.
#   "dot"            - DNS-over-TLS (RFC 7858), normally :853.
#   "doh"            - DNS-over-HTTPS (RFC 8484), normally :443.
# The server serves TCP/53 by default (TCP_LISTENER_ENABLED), so no server change
# is needed to use TCP.
#
# DoT/DoH are opt-in DISGUISE transports: they make the resolver hop look like
# ordinary encrypted DNS where plain 53 is fingerprinted. "auto" never escalates
# into them — you choose them deliberately. Choosing one is not a commitment
# though: if the TLS port is blocked (a common censorship response) the client
# falls back to UDP and then TCP/53 on its own, so you degrade to the survival
# path instead of losing the tunnel. They require DOT_LISTENER_ENABLED /
# DOH_LISTENER_ENABLED on the server (both off by default).
RESOLVER_TRANSPORT = "auto"

# Encrypted-resolver settings (used only by "dot"/"doh").
# RESOLVER_TLS_SERVER_NAME is the SNI + certificate name presented to the
# resolver; leave empty to use the resolver IP (which then needs an IP SAN or a
# pin). Resolver entries keep their DNS port; these ports are used instead.
RESOLVER_TLS_SERVER_NAME = ""
RESOLVER_DOT_PORT = 853
RESOLVER_DOH_PORT = 443
RESOLVER_DOH_PATH = "/dns-query"

# Trust model, best first:
#   1. RESOLVER_TLS_PIN - base64 SHA-256 of the server certificate's
#      SubjectPublicKeyInfo. Pinning replaces CA validation, so a self-signed or
#      private-CA server is trusted EXACTLY and nothing else is. Accepts a bare
#      hash or a "sha256/..." prefix. This is the right way to use a server that
#      fell back to a self-signed certificate.
#   2. empty pin (default) - normal hostname/CA verification.
#   3. RESOLVER_TLS_INSECURE_SKIP_VERIFY - last resort, off by default.
# The tunnel payload is AEAD-encrypted regardless, so option 3 never exposes
# traffic; prefer pinning anyway, because an unverified hop can be transparently
# intercepted and that interception is itself a censorship signal.
RESOLVER_TLS_PIN = ""
RESOLVER_TLS_INSECURE_SKIP_VERIFY = false

# Per-resolver adaptive rate limiting. When a resolver signals overload (DNS
# RCODE != 0 — REFUSED/SERVFAIL — or repeated timeouts) it is briefly cooled down
# and deprioritized so its load shifts to resolvers with headroom; sustained
# success fully restores it. This is redistribution, not a global throttle: it is
# self-gating (does nothing to healthy resolvers) and never idles the client (a
# packet still goes out over a paced resolver if none else is free). It also
# avoids triggering resolver/IP rate-limit blocks. Default on.
RESOLVER_RATE_LIMIT_ENABLED = true

# DNS-injection hardening (client-only, NO server change; zero speed cost).
# On restrictive networks a DPI box races a forged NXDOMAIN back to you while the
# real authoritative answer still arrives — so the resolver stays usable, but the
# client used to count that forgery as a failure, letting the censor throttle and
# disable perfectly good resolvers for free (which is what actually slows you
# down). When true (default), a NXDOMAIN carrying no tunnel payload is ignored as
# injection noise: it never throttles or disables the resolver, and the pending
# query is left open so the genuine answer is still scored as a success. Real
# unreachability is still caught by the one signal injection cannot forge — the
# query timing out with no real answer. Set false to restore the old behavior of
# counting every NXDOMAIN as a failure.
RESOLVER_IGNORE_INJECTED_NXDOMAIN = true

# QNAME reshaping: target maximum DNS label length (1..63) when laying the tunnel
# payload into the query name. 63 (default) packs the payload into the fewest,
# longest labels for maximum capacity. A smaller value (e.g. 32) produces
# shorter, more numerous, jittered labels that look less like the classic
# single-long-label DNS tunnel, at the cost of some payload capacity per query.
# Server-transparent: the server reassembles the labels regardless of how they
# are split, so this needs no server change and cannot desync.
QNAME_LABEL_LENGTH = 63

# ---- DNS query shaping (client-only, NO server change needed) -----------------
# These make each outgoing DNS query look more like a real stub resolver's query
# on the client->resolver leg (where restrictive-network DPI observes it). All
# four are server-transparent: the server echoes the transaction ID without
# validating it, lowercases the whole query name before decoding, and never even
# sees the EDNS cookie (the recursive resolver strips it). You can toggle any of
# them without touching or redeploying the server.

# Randomize the DNS transaction ID per query instead of using a sequential
# counter. A monotonically increasing ID to one zone is a tunnel tell (and a mild
# spoofing weakness); real stub resolvers randomize it. Recommended on.
DNS_RANDOMIZE_QUERY_ID = true

# Add an RFC 7873 EDNS Client Cookie (8 random bytes) to each query's OPT record,
# so the query looks like a modern stub. The recursive resolver terminates EDNS,
# so this only affects the client->resolver leg and never reaches the server.
# Recommended on.
DNS_EDNS_COOKIE = true

# DNS 0x20 mixed-case encoding: randomize the letter case of the query name. The
# server lowercases the name before decoding, so it never desyncs. It makes the
# all-lowercase base32 payload look less uniform, but does NOT reduce label
# entropy, so the anti-detection benefit is modest. Opt-in; default off.
DNS_QNAME_CASE_RANDOMIZATION = false

# Requestor's UDP payload size advertised in the EDNS OPT record (bytes),
# clamped to [512, 4096]. Larger lets resolvers return larger answers, which the
# high download MTU needs for throughput; a smaller value (e.g. 1232) looks more
# like a modern stub but can cap the answer size and hurt throughput. Keep 4096
# unless you specifically need a smaller advertised buffer.
EDNS_UDP_SIZE = 4096

# ---- Packet duplication -------------------------------------------------------
# All tunnel packets are sent by the client as DNS queries, so every
# duplicated packet consumes upload bandwidth. The "direction" below refers
# to what the packet's payload represents:
#
#   - Upload data  : STREAM_DATA, STREAM_RESEND
#                    (bulk bytes from you -> internet; duplication cost scales
#                    with your real upload traffic)
#   - Download ACK : STREAM_DATA_ACK, STREAM_DATA_NACK
#                    (tiny acknowledgements for bytes you are receiving;
#                    duplicating them barely costs upload bandwidth but
#                    improves download reliability on lossy links)
#   - Upload setup : STREAM_SYN, SOCKS5_SYN
#                    (small, upload-path critical)
#   - Download setup/control : PACKED_CONTROL_BLOCKS, STREAM_CLOSE_READ,
#                              STREAM_CLOSE_WRITE
#                              (small, reliability-sensitive control/teardown)
#
# TIP: On links with very limited upload bandwidth (e.g., typical Iranian ISP
# conditions) you usually want:
#   UPLOAD_PACKET_DUPLICATION_COUNT        = 1 or 2
#   DOWNLOAD_PACKET_DUPLICATION_COUNT      = 4 .. 7
#   UPLOAD_SETUP_PACKET_DUPLICATION_COUNT  = 2 .. 4
#   DOWNLOAD_SETUP_PACKET_DUPLICATION_COUNT= 4 .. 8

# Duplication for upload data packets (STREAM_DATA, STREAM_RESEND).
# Clamped in code to [1, 8].
UPLOAD_PACKET_DUPLICATION_COUNT = 3

# Duplication for download-ACK packets (STREAM_DATA_ACK, STREAM_DATA_NACK).
# Clamped in code to [1, 8].
DOWNLOAD_PACKET_DUPLICATION_COUNT = 7

# Duplication for upload setup packets (STREAM_SYN, SOCKS5_SYN).
# Clamped in code to [UPLOAD_PACKET_DUPLICATION_COUNT, 8].
UPLOAD_SETUP_PACKET_DUPLICATION_COUNT = 4

# Duplication for download setup / control packets
# (PACKED_CONTROL_BLOCKS, STREAM_CLOSE_READ, STREAM_CLOSE_WRITE).
# Clamped in code to [DOWNLOAD_PACKET_DUPLICATION_COUNT, 8].
DOWNLOAD_SETUP_PACKET_DUPLICATION_COUNT = 8

# Stream-aware resolver failover.
# If a stream keeps resending on the same preferred resolver, it can move.
# Threshold is clamped to [1, 128].
STREAM_RESOLVER_FAILOVER_RESEND_THRESHOLD = 1

# Minimum seconds between preferred-resolver switches for the same stream.
# Clamped to [0.1s, 120s].
STREAM_RESOLVER_FAILOVER_COOLDOWN = 0.5

# If true, resolvers rejected during initial MTU testing are rechecked in the
# background using the current synced upload/download MTUs.
RECHECK_INACTIVE_SERVERS_ENABLED = true

# Full background recheck cycle interval.
# Clamped to [60s, 86400s].
RECHECK_INACTIVE_INTERVAL_SECONDS = 30.0

# Delay between background rechecks of individual resolver entries.
# Clamped to [1s, 600s].
RECHECK_SERVER_INTERVAL_SECONDS = 1.0

# Number of inactive/runtime-disabled resolver entries rechecked per cycle.
# Clamped to [1, 1024].
RECHECK_BATCH_SIZE = 30

# If true, a resolver is runtime-disabled when it stays timeout-only across the
# configured window and enough observations have been collected.
AUTO_DISABLE_TIMEOUT_SERVERS = true

# Timeout-only observation window in seconds.
# Clamped to [1s, 86400s].
AUTO_DISABLE_TIMEOUT_WINDOW_SECONDS = 90.0

# Minimum observations required before auto-disable can trigger.
# Clamped to [1, 10000].
AUTO_DISABLE_MIN_OBSERVATIONS = 3

# Frequency of auto-disable evaluation.
# Clamped to [0.25s, 600s].
AUTO_DISABLE_CHECK_INTERVAL_SECONDS = 1.0

# If true, payload labels are base-encoded before tunneling.
# Usually keep false unless a specific resolver path behaves better with it.
BASE_ENCODE_DATA = false

# Use the one-byte session-ID wire format for legacy MasterDNS/StormDNS/
# WhiteDNS servers. Keep false for native XDNS servers.
LEGACY_SESSION_ID = false

# Maximum concurrent local SOCKS/TCP/UDP streams. 0 disables the cap.
MAX_ACTIVE_STREAMS = 2048

# Prevent a stalled local app from holding an incomplete SOCKS handshake.
LOCAL_HANDSHAKE_TIMEOUT_SECONDS = 5.0

# ------------------------------------------------------------------------------
# 5) Compression
# What this section does:
# - Controls payload compression negotiation for upload and download directions.
# ------------------------------------------------------------------------------

# Allowed values:
# 0 = OFF
# 1 = ZSTD
# 2 = LZ4
# 3 = ZLIB
UPLOAD_COMPRESSION_TYPE = 2
DOWNLOAD_COMPRESSION_TYPE = 2

# Minimum payload size before compression is attempted.
# Must be >= 1.
COMPRESSION_MIN_SIZE = 120

# ------------------------------------------------------------------------------
# 6) MTU Discovery
# What this section does:
# - Controls initial resolver validation and MTU discovery.
# ------------------------------------------------------------------------------

# Minimum accepted upload/download MTUs after testing.
# 0 can be used to effectively disable a bound, but keep realistic values.
MIN_UPLOAD_MTU = 100
MIN_DOWNLOAD_MTU = 1000

# Initial maximum MTU search bounds.
# Must be >= the matching MIN value if both are set.
MAX_UPLOAD_MTU = 200
MAX_DOWNLOAD_MTU = 4000

# MTU probe retries, timeout, and parallelism.
# Separate values are used depending on STARTUP_MODE so a fast
# log-based startup can use lighter probes than a full resolvers scan.
# Retries and parallelism must be >= 1.
MTU_TEST_RETRIES_RESOLVERS = 3
MTU_TEST_TIMEOUT_RESOLVERS = 2.0
MTU_TEST_PARALLELISM_RESOLVERS = 100
MTU_TEST_RETRIES_LOGS = 5
MTU_TEST_TIMEOUT_LOGS = 2.0
MTU_TEST_PARALLELISM_LOGS = 32

# Adaptive per-group MTU (loss-aware probing + resolver clustering).
# MTU_PROBE_SAMPLES > 1 switches MTU probing from "accept if any retry succeeds"
# to loss-aware: each candidate MTU is probed this many times and accepted only
# if its measured loss fraction is <= MTU_MAX_LOSS. Larger samples give a more
# accurate per-resolver MTU at the cost of more probe traffic during startup.
# = 1 (default) keeps the classic retry behavior.
MTU_PROBE_SAMPLES = 1
MTU_MAX_LOSS = 0.0
# Resolver clustering threshold: after probing, resolvers are grouped into MTU
# bands; a new group starts wherever the gap between consecutive viable download
# MTUs exceeds this fraction of the larger value (0.25 = 25%).
MTU_GROUP_GAP_RATIO = 0.25
# When true (default), the client runs the session at the throughput-optimal MTU
# (the MTU D that maximizes D x number-of-resolvers-that-sustain-D) instead of
# the global minimum. Resolvers that cannot sustain it are not dropped: they are
# kept as backups (failover) and only carry traffic if the whole active pool
# fails. This stops a few slow resolvers from throttling everyone while still
# keeping them in reserve. Set false to use the legacy global-minimum MTU across
# all valid resolvers.
MTU_ADAPTIVE_GROUPING = true
# Applies only when RESOLVER_BALANCING_STRATEGY = 5 (MTU-weighted). In that mode
# the session runs at the HIGHEST MTU a viable subset of resolvers can sustain,
# rather than the throughput-optimal common denominator, so resolvers that support
# a higher MTU actually use it. This is the minimum number of resolvers that must
# sustain the raised MTU before it is adopted — a redundancy floor so a single
# fast resolver cannot strand the session. Higher = safer/lower MTU, lower =
# more aggressive/higher MTU. Default 2.
MTU_WEIGHTED_MIN_POOL = 2

# Start once seven resolvers (or the whole smaller fleet) pass MTU probing.
# Remaining resolvers are tested in the background at low priority.
FAST_CONNECT = false

# ------------------------------------------------------------------------------
# 7) Runtime Workers, Queues, and Timers
# What this section does:
# - Controls async reader/writer/processor worker counts.
# - Controls queue sizes and dispatcher polling.
# - Controls stream cleanup retention and local UDP associate timeout.
# ------------------------------------------------------------------------------

# Async tunnel runtime worker counts.
# Each must be >= 1.
RX_TX_WORKERS = 4
TUNNEL_PROCESS_WORKERS = 4

# Per-packet tunnel timeout used in async runtime paths.
# Clamped to [0.5s, 120s].
TUNNEL_PACKET_TIMEOUT_SECONDS = 10.0

# Dispatcher idle polling interval.
# Lower values reduce wake latency but consume more CPU.
# Clamped to [0.001s, 1s].
DISPATCHER_IDLE_POLL_INTERVAL_SECONDS = 0.020

# Channel sizes for async TX/RX pipelines.
# Clamped in code to [64, 65536].
TX_CHANNEL_SIZE = 2048
RX_CHANNEL_SIZE = 2048

# UDP connection pool size per resolver key.
# Clamped to [1, 1024].
RESOLVER_UDP_CONNECTION_POOL_SIZE = 256

# Initial queue/store capacities.
# These reduce reallocations under many active streams.
STREAM_QUEUE_INITIAL_CAPACITY = 512
ORPHAN_QUEUE_INITIAL_CAPACITY = 128
DNS_RESPONSE_FRAGMENT_STORE_CAPACITY = 1024

# Local UDP ASSOCIATE read timeout.
# Clamped to [1s, 3600s].
SOCKS_UDP_ASSOCIATE_READ_TIMEOUT_SECONDS = 120.0

# How long terminal streams are retained before cleanup.
# Clamped to [1s, 3600s].
CLIENT_TERMINAL_STREAM_RETENTION_SECONDS = 45.0

# How long cancelled setup streams are retained before cleanup.
# Clamped to [1s, 3600s].
CLIENT_CANCELLED_SETUP_RETENTION_SECONDS = 120.0

# Session init retry profile.
# These shape retry delays after failed init/reset attempts.
# Base/step/max are durations; linear_after is a count threshold.
SESSION_INIT_RETRY_BASE_SECONDS = 1.0
SESSION_INIT_RETRY_STEP_SECONDS = 1.0
SESSION_INIT_RETRY_LINEAR_AFTER = 5
SESSION_INIT_RETRY_MAX_SECONDS = 60.0

# Retry delay after SESSION_BUSY.
# Clamped to [1s, 3600s].
SESSION_INIT_BUSY_RETRY_INTERVAL_SECONDS = 60.0

# How many distinct resolvers one SESSION_INIT is raced across.
# Racing turns a slow serial "try one, wait a full timeout, try the next"
# connect into "ask a few at once, take the first reply", which is a large win
# on lossy networks where any single resolver may be dead. The server keys
# sessions by the init signature and reuses within SESSION_INIT_REUSE_TTL, so
# the same init arriving via several resolvers still yields exactly one session.
# Higher values cost that many queries per attempt. 1 disables racing.
# Clamped to [1, 8]. Default: 3.
SESSION_INIT_RACING_COUNT = 3

# Ping/keepalive pacing.
# These control how aggressively the client sends ping traffic depending on
# recent activity.
PING_AGGRESSIVE_INTERVAL_SECONDS = 0.200
PING_LAZY_INTERVAL_SECONDS = 0.750
PING_COOLDOWN_INTERVAL_SECONDS = 2.0
PING_COLD_INTERVAL_SECONDS = 15.0
PING_WARM_THRESHOLD_SECONDS = 5.0
PING_COOL_THRESHOLD_SECONDS = 15.0
PING_COLD_THRESHOLD_SECONDS = 30.0

# Watchdog: if the client has been actively sending non-ping traffic but
# receives no server response at all for this long, the session is forcibly
# restarted. This recovers the "zombie" state where all resolvers are
# silently dropping packets and no server-initiated reset (REJECT / BUSY /
# ERROR_DROP) ever arrives. Clamped to [10s, 3600s]. Set to 0 to disable.
PING_WATCHDOG_TIMEOUT_SECONDS = 300.0

# ------------------------------------------------------------------------------
# 8) ARQ Reliability & Packing
# What this section does:
# - Controls data/control retransmission behavior.
# - Controls per-stream buffering limits and batch packing.
# ------------------------------------------------------------------------------

# Maximum control blocks packed together in one outgoing batch.
# Clamped to [1, 64].
MAX_PACKETS_PER_BATCH = 8

# ARQ data/control behavior.
# These values are clamped in code and should stay internally consistent.
ARQ_WINDOW_SIZE = 1000
ARQ_INITIAL_RTO_SECONDS = 0.6
ARQ_MAX_RTO_SECONDS = 3.0
ARQ_CONTROL_INITIAL_RTO_SECONDS = 0.5
ARQ_CONTROL_MAX_RTO_SECONDS = 2.0
ARQ_MAX_CONTROL_RETRIES = 120
ARQ_INACTIVITY_TIMEOUT_SECONDS = 600.0
ARQ_DATA_PACKET_TTL_SECONDS = 2400.0
ARQ_CONTROL_PACKET_TTL_SECONDS = 1200.0
ARQ_MAX_DATA_RETRIES = 120

# Maximum out-of-order gap ahead of rcvNxt that can trigger STREAM_DATA_NACK.
# 0 disables NACK generation entirely.
# Keep this small so only near-miss gaps cause early resend requests.
ARQ_DATA_NACK_MAX_GAP = 64

# Minimum seconds between repeated NACKs for the same missing sequence number.
# Clamped to [0.1s, 30s].
ARQ_DATA_NACK_INITIAL_DELAY_SECONDS = 0.4
ARQ_DATA_NACK_REPEAT_SECONDS = 0.8

ARQ_TERMINAL_DRAIN_TIMEOUT_SECONDS = 120.0
ARQ_TERMINAL_ACK_WAIT_TIMEOUT_SECONDS = 90.0

# ------------------------------------------------------------------------------
# 9) Logging
# What this section does:
# - Controls console/file logger verbosity.
# - Controls per-session log file creation.
# - When LOG_TO_FILE is true, each session also writes a structured
#   resolver_cache_<timestamp>.log file to LOG_DIR containing the working
#   resolvers and their MTU values, which is consumed by the log-based startup
#   path (see Section 10).
# ------------------------------------------------------------------------------

# Typical values: DEBUG, INFO, WARN, ERROR
LOG_LEVEL = "INFO"

# Console presentation:
#   "auto"  = organized interactive dashboard on a real terminal, plain logs
#             under systemd, redirection, desktop embedding, and scan-only mode.
#   "tui"   = force the cross-platform Bubble Tea dashboard.
#   "plain" = always use traditional line-oriented logs.
TERMINAL_UI = "auto"

# When true, the session log and the resolver-cache statistics log are both
# written to LOG_DIR.  Enabled by default so that logs from each session are
# available for review and for log-based startup (see Section 10).
LOG_TO_FILE = true

# Directory (relative to the config file, or absolute) where log files are stored.
LOG_DIR = "logs"

# File name template for the session log file.
# {time} is replaced with the startup timestamp (YYYYMMDD_HHMMSS).
LOG_FILE_NAME = "XDNS_{time}.log"

# Interval in seconds at which upload/download speed and cumulative session
# totals are printed to the console log window.
# Set to 0 to disable the stats reporter entirely.
# When > 0, the value is clamped to [1, 3600].
STATS_REPORT_INTERVAL_SECONDS = 10.0

# ------------------------------------------------------------------------------
# 10) Log-Based Startup
# What this section does:
# - Controls whether the app can start from previous session log files instead
#   of running a full resolver scan, dramatically reducing startup time.
# - The resolver cache log records every working resolver + its MTU values at
#   the end of each successful MTU test. On subsequent launches the app can
#   restore those values directly.
# ------------------------------------------------------------------------------

# Startup mode.
# Allowed values:
#   "ask"       = prompt the user each time (auto-selects option 1 after 10 s)
#   "resolvers" = always start from client_resolvers.txt (full scan, no prompt)
#   "logs"      = always start from log files (no prompt; falls back to full
#                 scan when no usable log entries are found)
STARTUP_MODE = "resolvers"

# Maximum age (in days) of resolver cache log files that will be considered
# during log-based startup.  0 means use all files regardless of age.
LOG_SCAN_MAX_DAYS = 30

# Maximum number of resolvers to load from log files.
# Resolvers are ranked by quality (appear-count × 1000 + DOWN-MTU + UP-MTU).
# 0 means use all found resolvers.
LOG_SCAN_MAX_RESOLVERS = 0

# When true, even a log-based startup will run the full MTU scan to verify
# that the previously-known resolvers are still reachable.
# When false (default), MTU testing is skipped and pre-known values are used
# directly, giving the fastest possible startup time.
LOG_BASED_MTU_VERIFY = true

# ------------------------------------------------------------------------------

CONFIG_VERSION = "1"
